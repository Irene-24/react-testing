import React from "react";
import { shallow } from "enzyme";
import Header from "./index";

describe( "Haeder component", () => 
{
    it( "Should render header without error", () =>
    {
        expect( 1 ).toBe( 2 );

    } );

}
);
